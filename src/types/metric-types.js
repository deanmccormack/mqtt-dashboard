// metric
export const CLUSTER_SIZE = 'meta/clustersize';

export const NETWORKING_BYTES_WRITE_TOTAL = `networking.bytes.write.total`;
export const MSG_INCOMING_TOTAL_COUNT = `messages.incoming.total.count`;
export const MSG_OUTGOING_TOTAL_COUNT = `messages.outgoing.total.count`;
export const MSG_QUEUED_COUNT = `messages.queued.count`;
export const MSG_RETAINED_CURRENT_COUNT = `messages.retained.current`;

export const NETWORKING_BYTES_READ_TOTAL = `networking.bytes.read.total`;
export const NETWORKING_CONNECTIONS_CURRENT = `networking.connections.current`;

export const SESSSIONS_OVERALL_COUNT = `sessions.overall.current`
export const SUB_OVERALL_CURRENT_COUNT = `subscriptions.overall.current`;

export const TIME_STAMP = `time.stamp`;
